<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EventLocator" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/primaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="progressBarStyle">@style/MyProgressBar</item>

    </style>

    <style name="Theme.EventLocator.NoActionBar" parent = "Theme.EventLocator">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MyProgressBar" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:backgroundTint">@color/primaryColor</item>
        <item name="android:indeterminateTint">@color/primaryTextColor</item>
    </style>

    <style name="MyChipStyle" parent="@style/Widget.MaterialComponents.Chip.Filter">
        <item name="chipBackgroundColor">@color/chip_background_color</item>
        <item name="android:textColor">@color/chip_text_color</item>
        <item name="chipStrokeWidth">4dp</item>
        <item name="chipStrokeColor">@color/primaryColor</item>
        <item name="chipIconEnabled">false</item>
        <item name="checkedIcon">@null</item>
    </style>
</resources>